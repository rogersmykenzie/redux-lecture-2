// Breaking Bad Ducks

// Initial State

const initialState = {
  characters: [],
  loading: true
};

// Action Types Go Here

// Reducer
function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
